// //Soporte para archivos http
// /**
// Aunque no es una mejora específica de ASP.NET Core sino del entorno de desarrollo, vale la pena dedicar una lección a conocer el nuevo cliente HTTP integrado en Visual Studio (y también disponible en otros entornos como Visual Studio Code vía plugins o extensiones).

// Esta nueva herramienta permite, sin abandonar el IDE, componer y lanzar peticiones HTTP contra un servidor, una tarea bastante habitual para la que antes debíamos recurrir a herramientas externas como Postman, Fiddler o incluso utilidades basadas en CLI o línea de comandos.
// */
// var builder = WebApplication.CreateBuilder(args);

// var app = builder.Build();

// app.Run();